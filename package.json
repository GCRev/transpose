{
  "name": "transpose",
  "version": "1.0.0",
  "description": "transpose files from columns to rows",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gheld",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.2.4",
    "eslint": "^7.24.0"
  }
}
